# Verification — reserved

향후 requirement/test 정의와 runtime observation을 받아 판정하는 순수 로직을 둡니다.
UI와 graphics, Rapier 내부 객체를 참조하지 않습니다. data/mock/를 입력으로 사용하지 않습니다.
현재 oracle, ISO requirement, PASS/FAIL 판정을 구현하지 않았습니다.
